<div id="clippy-container" data-phase="hidden">
  <!-- Loading dot -->
  <div id="clippy-dot" class="clippy-dot" title="Loading..."></div>

  <!-- Character -->
  <div id="clippy-character" class="clippy-character" title="It looks like you need help...">
    <svg viewBox="0 0 60 120" xmlns="http://www.w3.org/2000/svg" class="clippy-svg">
      <!-- Paperclip body -->
      <path d="M30 8 
               C48 8, 54 22, 54 38
               L54 88
               C54 108, 6 108, 6 88
               L6 32
               C6 16, 24 16, 24 32
               L24 76
               C24 84, 36 84, 36 76
               L36 42
               C36 32, 48 32, 48 42
               L48 94" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="7"
            stroke-linecap="round"/>
      <!-- Left eye -->
      <ellipse cx="22" cy="14" rx="4" ry="5" fill="white"/>
      <circle cx="23" cy="14" r="2" fill="black"/>
      <!-- Right eye -->
      <ellipse cx="38" cy="14" rx="4" ry="5" fill="white"/>
      <circle cx="37" cy="14" r="2" fill="black"/>
    </svg>
  </div>

  <!-- Speech bubble -->
  <div id="clippy-bubble" class="clippy-bubble">
    <button id="clippy-close" class="clippy-close" aria-label="Dismiss Clippy">×</button>
    <p id="clippy-text" class="clippy-text"></p>
    <div class="clippy-tail"></div>
  </div>
</div>

<script>
  import { initClippy } from "../client/clippy.ts";
  initClippy();
</script>

<style>
  #clippy-container {
    position: fixed;
    bottom: 1.5rem;
    right: 1.5rem;
    z-index: 100;
    font-family: var(--font-display);
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  #clippy-container[data-phase="booting"],
  #clippy-container[data-phase="materializing"],
  #clippy-container[data-phase="active"],
  #clippy-container[data-phase="dismissed"] {
    opacity: 1;
    pointer-events: auto;
  }

  /* Loading dot */
  .clippy-dot {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-hot-pink);
    box-shadow: 0 0 8px var(--color-hot-pink);
    opacity: 0;
    transform: scale(0);
    transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
    cursor: wait;
  }

  #clippy-container[data-phase="booting"] .clippy-dot {
    opacity: 1;
    transform: scale(1);
    animation: dot-pulse 1.2s ease-in-out infinite;
  }

  #clippy-container[data-phase="materializing"] .clippy-dot {
    opacity: 0;
    transform: scale(2);
    transition: all 0.4s ease;
  }

  @keyframes dot-pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.7; }
  }

  /* Character */
  .clippy-character {
    position: relative;
    width: 0;
    height: 0;
    opacity: 0;
    transform: scale(0) rotate(-20deg);
    transition: all 0.7s cubic-bezier(0.34, 1.56, 0.64, 1);
    cursor: pointer;
    filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.5));
  }

  #clippy-container[data-phase="materializing"] .clippy-character,
  #clippy-container[data-phase="active"] .clippy-character,
  #clippy-container[data-phase="dismissed"] .clippy-character {
    width: 50px;
    height: 100px;
    opacity: 1;
    transform: scale(1) rotate(0deg);
  }

  #clippy-container[data-phase="active"] .clippy-character {
    animation: clippy-float 3s ease-in-out infinite;
  }

  #clippy-container[data-phase="dismissed"] .clippy-character {
    width: 24px;
    height: 48px;
    opacity: 0.6;
    transform: scale(1);
    animation: none;
  }

  #clippy-container[data-phase="dismissed"] .clippy-character:hover {
    opacity: 1;
    transform: scale(1.1);
  }

  @keyframes clippy-float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    25% { transform: translateY(-4px) rotate(1deg); }
    75% { transform: translateY(-2px) rotate(-1deg); }
  }

  .clippy-svg {
    width: 100%;
    height: 100%;
    color: var(--color-text);
    transition: color 0.3s;
  }

  .clippy-character:hover .clippy-svg {
    color: var(--color-hot-pink);
  }

  /* Speech bubble */
  .clippy-bubble {
    position: absolute;
    bottom: 110%;
    right: 0;
    width: 260px;
    padding: 0.75rem 1rem;
    background: var(--color-card-bg);
    border: 3px ridge var(--color-border);
    opacity: 0;
    transform: translateY(10px) scale(0.95);
    transform-origin: bottom right;
    transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
    pointer-events: none;
  }

  .clippy-bubble.visible {
    opacity: 1;
    transform: translateY(0) scale(1);
    pointer-events: auto;
  }

  .clippy-bubble::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--color-hot-pink), var(--color-cyan), var(--color-yellow));
    background-size: 200% 100%;
    animation: marquee-bg 3s linear infinite;
  }

  .clippy-close {
    position: absolute;
    top: 4px;
    right: 6px;
    background: none;
    border: none;
    color: var(--color-text-secondary);
    font-family: var(--font-sans);
    font-size: 1.1rem;
    line-height: 1;
    cursor: pointer;
    padding: 0 4px;
    z-index: 2;
  }

  .clippy-close:hover {
    color: var(--color-red);
  }

  .clippy-text {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--color-text);
    margin: 0;
    padding-right: 1rem;
    min-height: 1.5em;
  }

  .clippy-tail {
    position: absolute;
    bottom: -10px;
    right: 20px;
    width: 0;
    height: 0;
    border-left: 10px solid transparent;
    border-right: 10px solid transparent;
    border-top: 10px solid var(--color-border);
  }

  .clippy-tail::after {
    content: "";
    position: absolute;
    top: -13px;
    left: -8px;
    width: 0;
    height: 0;
    border-left: 8px solid transparent;
    border-right: 8px solid transparent;
    border-top: 8px solid var(--color-card-bg);
  }

  /* Typing cursor effect */
  .clippy-text.typing::after {
    content: "|";
    animation: cursor-blink 1s step-end infinite;
    color: var(--color-accent);
    margin-left: 2px;
  }

  @keyframes cursor-blink {
    0%, 49% { opacity: 1; }
    50%, 100% { opacity: 0; }
  }

  /* Mobile */
  @media (max-width: 600px) {
    .clippy-bubble {
      width: 200px;
      right: -10px;
    }
    #clippy-container {
      bottom: 1rem;
      right: 1rem;
    }
  }
</style>